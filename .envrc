use flake

if has just; then
    echo ""
    echo "=== Pico 2 W Development Environment ==="
    echo ""
    echo "Use 'just' for all project operations:"
    echo "  just envsetup  - Setup SDK and OpenOCD"
    echo "  just build     - Build application"
    echo "  just flash     - Flash to device"
    echo "  just run       - Build and flash"
    echo "  just capture   - Capture serial output"
    echo "  just           - Show all commands"
    echo ""
fi
